#include <iostream>
#include <time.h>
using namespace std;

int main()
{
    int coord[10][2];
    int number[20];

    // srand(time(NULL));

    // for (int i = 0; i < 10; i++)
    // {
    //     int xc = (rand() % 20) + 1;
    //     int yc = (rand() % 20) + 1;
    //     coord[i][0] = xc;
    //     coord[i][1] = yc;
    // }

    // for (int i = 0; i < 10; i++)
    // {
    //     cout << "X = " << coord[i][0] << " Y = " << coord[i][1] << endl;
    // }

    for (int i = 1; i <= 20; i++)
    {
        number[i] = i;
    }

    for (int i = 0; i < 20; i++){
        
    }
}